node_modules/
dist/

# Local run artifacts (screenshots, logs, etc.)
artifacts/
*.png

# Keep Playwright browser binaries repo-local (large; do not commit)
工程依赖/Playwright/browsers/
!工程依赖/Playwright/browsers/.gitkeep
